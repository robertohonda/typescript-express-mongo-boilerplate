// import { NextFunction, Request, Response } from "express";

// interface IHandler {
//   static handleResponse: (promise: Promise<any>, req: Request, res: Response, next: NextFunction) => Promise<any>;
// }

// export default IHandler;
